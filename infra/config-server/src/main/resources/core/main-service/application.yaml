spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/maindb
    username: dbuser
    password: 12345
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
stat:
  server-id: stats-server
server:
  port: 0