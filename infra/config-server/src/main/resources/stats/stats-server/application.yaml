server:
  port: 0
spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        hibernate:
          format_sql: true
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:postgresql://localhost:5434/stats_db
    driver-class-name: "org.postgresql.Driver"
    username: "dbuser"
    password: "12345"